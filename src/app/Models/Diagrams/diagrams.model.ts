export interface Diagrams {
    id?: number | null,
    name?: string,
    description?: string,
    json_data?: string,
    s_bpartner_i_employee_id?: number | null,
    created_by?: number | null,
}
